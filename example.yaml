# Basic configuration
default_config:

# Load the HydroLink integration
hydrolink:
  # Configuration is done through the UI
  # No YAML configuration required

# Optional: Service example for manual regeneration
# Use this in your automations
# service: hydrolink.trigger_regeneration
# target:
#   entity_id: sensor.hydrolink_salt_level

# Optional: Enable debug logging
logger:
  default: info
  logs:
    custom_components.hydrolink: debug

# Optional: Example automation for low salt alert
# automation:
#   - alias: "HydroLink Low Salt Alert"
#     trigger:
#       platform: numeric_state
#       entity_id: sensor.hydrolink_salt_level
#       below: 20
#     action:
#       - service: notify.mobile_app
#         data:
#           title: "Low Salt Level"
#           message: "Water softener salt level is below 20%"